#include	<stdio.h>

int	main(void)
{
	int		n;
	int		m;
	int		result;
	char	s[1000001];
	int		start;
	int		end;

	scanf("%d", &n);
	scanf("%d", &m);
	scanf("%s", s);

	for (int i = 0; i < m; i++)
	{
		if (s[i] == 'I')
		{
			end = i + 2;
			for (int j = 0; j < n; j++)
			{
				if ()
			}
		}
	}
	printf("%d\n", result);
	return (0);
}
